{
  "name": "so-tra-tu-test",
  "private": true,
  "description": "Kiểm tra hai bản trang dựng ra bằng một DOM giả (jsdom).",
  "scripts": {
    "test": "node run.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.0.1"
  }
}
